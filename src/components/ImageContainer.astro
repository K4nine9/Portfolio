---
const { src, alt, caption } = Astro.props;
---

<div class="mt-6 mb-6 border border-gray-200 bg-gray-100 p-2 rounded-lg">
    <img src={src} alt={alt} class="w-full h-auto" />
    <p class="text-sm text-gray-500 mt-2 text-center">{caption}</p>
</div>
